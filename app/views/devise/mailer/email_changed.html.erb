<p>こんにちは <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>メールが<%= @resource.unconfirmed_email %>に変更されることを通知するためにご連絡いたします</p>
<% else %>
  <p>メールアドレスが<%= @resource.email %>に変更されたことをお知らせするためにご連絡いたします</p>
<% end %>
