<%= form_for(current_user.active_relationships.build, remote: true, html: { class: "follow_class"}) do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit "Follow", class: "follow-btn" %>
<% end %>
