version: 2.1
jobs:
  test:
    machine:
      image: circleci/classic:edge
    steps:
      - checkout
      - run:
          name: docker-compose build
          command: docker-compose build
      - run:
          name: bundle install
          command: docker-compose exec techbooks_app_1 bundle install
      - run:
          name: setup db
          command: docker-compose exec techbooks_app_1 rails db:create db:migrate
      - run:
          name: seed db
          command: docker-compose exec techbooks_app_1 rails db:seed
      - run:
          name: docker-compose up
          command: docker-compose up -d
      - run:
          name: rspec
          command: docker-compose exec techbooks_app_1 rspec
      - run:
          name: docker-compose down
          command: docker-compose down


workflows:
  test_workflow:
    jobs:
      - test:
        filters:
          branches:
            only:
            - task-1
